import { Box, List } from "@mui/material";

const Questions = () => {
  return (
    <List
      sx={{
        width: "100%",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
      }}
    ></List>
  );
};

export default Questions;
